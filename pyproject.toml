[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["backend", "serverless"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]
